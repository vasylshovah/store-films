import React from "react";
import {withRouter} from "react-router-dom"

const ProfilePage = () => {
    return (
        <div>If you see this, its good</div>
    )
}

export default withRouter(ProfilePage)